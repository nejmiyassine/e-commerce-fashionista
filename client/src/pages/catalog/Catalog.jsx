import CatalogSidebar from '../../components/Catalog/CatalogSidebar';

const Catalog = () => {
    return (
        <div>
            <CatalogSidebar />
        </div>
    );
};

export default Catalog;
